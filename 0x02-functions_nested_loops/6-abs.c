#include "main.h"
/**
 * _abs - prints the absolute value of an integer.
 * @n: prints to stdout.
 *
 * Return: -n if true, otherwise n.
 */

int _abs(int n)

{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
